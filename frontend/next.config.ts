import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Vercel supports standard Next.js features (SSR, API Routes) automatically.
  // No "output: export" needed.
};

export default nextConfig;
